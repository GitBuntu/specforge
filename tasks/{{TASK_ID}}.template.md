# 🚫 STOP — THIS IS A SPECFORGE TEMPLATE

This file is a **TEMPLATE**, not an artefact.

**LLM and engineer rules:**
- Do NOT edit this file.
- Do NOT fill in this file.
- Do NOT update this file.
- Do NOT continue this file.
- Do NOT "start work" in this file.
- Do NOT generate content inside this file.
- Do NOT overwrite this file.

When asked to use this template:
1. READ ONLY.
2. Create a **NEW** file in the correct folder.
3. Populate the **NEW** file.
4. Leave this template unchanged forever.

## Strict Sequencing Rule
- **DO NOT create implementation files until the complete SpecForge chain is defined.**
- The chain MUST be completed in this exact order:
  1. Context (01-*.md)
  2. Requirement (REQ-*.md) 
  3. Feature (FEATURE-*.md)
  4. Scenario (SCENARIO-*.md)
  5. Test (TEST-*.md)
  6. Plan (PLAN-*.md)
  7. Tasks (TASK-*.md)
- Implementation code is created ONLY AFTER all 7 SpecForge artifacts exist.
- Any deviation from this sequence violates the template.

---

# Task {{TASK_ID}} for {{FEATURE_NAME}}

## SpecForge Chain Position
This is the TEMPLATE for artefact 7 of 7.

SpecForge Chain:
Context → Requirements (1..N) → Feature → Scenario → Test → Plan → Task

You are defining: Task.

---

## TDD Cycle Phase (CRITICAL)
**Phase**: `[RED]` or `[IMPL]` (MUST specify — no blank values allowed)

- `[RED]`: This task writes a new failing test in `/tests/{{FEATURE_NAME}}/{{TEST_ID}}.md` (path: inherited from {{PLAN_ID}}.md)
  - Test must NOT pass before this task completes
  - After completing this [RED] task, test file becomes static (immutable)
- `[IMPL]`: This task writes implementation code to make ONE paired [RED] test pass
  - Implementation touches code only, NEVER test files
  - If paired [RED] test cannot pass → implementation is wrong (not the test)

**Pairing Rule** (REQUIRED):
- Each `[RED]` task MUST be paired with exactly one `[IMPL]` task
- Example sequence: TASK-001[RED], TASK-002[IMPL], TASK-003[RED], TASK-004[IMPL]
- [RED] → [IMPL] → [RED] → [IMPL] pattern is NON-NEGOTIABLE

---

## Test Immutability Constraint (TDD Discipline)

⚠️ **IF Phase = [RED]**:
- You are WRITING a new test file in `/tests/{{FEATURE_NAME}}/{{TEST_ID}}.md`
- This test MUST fail initially (proving test is real and validating something)
- Commit message MUST include `[RED]`: `test({{feature}}): [RED] {{behavior}}`
- Once committed, this test becomes a **static contract**
- NO modifications to test assertions after this [RED] commit are permitted

⚠️ **IF Phase = [IMPL]**:
- You are WRITING implementation code ONLY
- You MUST make the paired [RED] test pass
- You MUST NEVER modify any test files
- If the paired [RED] test cannot pass → implementation is wrong (not the test)
- Commit message format: `impl({{feature}}): {{behavior}}`

**Golden Rule**: Tests are immutable contracts. If implementation cannot make test pass, implementation fails—never modify test.

---

## SpecForge Contract
This artefact is governed by the SpecForge Contract. Strict enforcement rules:

1. Do NOT reinterpret any requirement. COPY Requirement Statement sections verbatim from /requirements/REQ-{{REQ_ID_X}}.md.
2. Do NOT summarise or simplify requirement text. COPY complete text without omission.
3. Do NOT infer missing behaviour. COPY ONLY what exists in requirements and tests.
4. Do NOT add assumptions. Reference ONLY assertions explicitly in {{TEST_ID_X}}.md Assertions sections.
5. Do NOT remove constraints. Include ALL assertions from {{TEST_ID_X}}.md that this task must satisfy.
6. Do NOT introduce new invariants, domain events, or states. Copy only from Context and requirements.
7. Do NOT code beyond test assertions. Task is complete when ALL test cases pass.
8. Do NOT reference infrastructure, frameworks, patterns, or technology. Use ONLY domain concepts from Context.
---

# Requirements (Verbatim Copy-Forward)
The following requirements MUST be copied forward exactly as written from source files:

- REQ-{{REQ_ID_1}}: [COPY EXACT Requirement Statement from ../requirements/REQ-{{REQ_ID_1}}.md — do NOT paraphrase, abbreviate, or reword]
- REQ-{{REQ_ID_2}}: [COPY EXACT Requirement Statement from ../requirements/REQ-{{REQ_ID_2}}.md — do NOT paraphrase, abbreviate, or reword]

**Strict Rule**: Copy complete Requirement Statement sections verbatim. Do NOT use "..." or partial quotes. These are the authoritative specifications that must be satisfied by this task.

---

# Scenarios and Tests Covered
This Task implements behaviour validated by tests listed below.

**Scenarios covered** (from Plan):
- {{SCENARIO_ID}}

**Test cases this task MUST satisfy** (from Plan):
- {{TEST_ID_1}} — from ../tests/{{FEATURE_NAME}}/{{TEST_ID_1}}.md
- {{TEST_ID_2}} — from ../tests/{{FEATURE_NAME}}/{{TEST_ID_2}}.md

**Strict Rules**:
- ALL {{TEST_ID_X}} and {{SCENARIO_ID}} values MUST be copied from {{PLAN_ID}}.md \u2014 do NOT invent or modify
- Verify EVERY test file exists before saving this task
- This task MUST make ALL listed test cases pass (zero skipped tests)
- If test file does not exist, this is a fatal error \u2014 do NOT proceed

---

# Task Description
This section describes WHAT must be done (not HOW). It must decompose directly from planning step assertions.
**Zero implementation decisions, zero design choices, zero infrastructure references.**

**REQUIRED FORMAT** (use EXACTLY this structure for each planning step this task addresses):
```
Planning Step Reference: [COPY planning step exactly from {{PLAN_ID}}.md Test-Driven Planning section]
Test Reference: {{TEST_ID_X}} (from `/tests/{{FEATURE_NAME}}/{{TEST_ID_X}}.md`)
Requirement: {{REQ_ID_X}} (from `/requirements/REQ-{{REQ_ID_X}}.md`)
Assertion: [COPY exact assertion syntax from {{TEST_ID_X}}.md Assertions section]
Approach: [ONE_SENTENCE_domain_focused — what domain behavior satisfies this assertion]
```

**Rules for each section:**
- `Planning Step Reference`: Copy ENTIRE planning step line from {{PLAN_ID}}.md
- `Test Reference`: Copy {{TEST_ID_X}} from the planning step
- `Requirement`: Copy {{REQ_ID_X}} from the planning step
- `Assertion`: Copy EXACTLY from test file Assertions section

**Format Example** (CORRECT):
```
Planning Step Reference: Step 1: Order must Total == sum of all OrderLine.Subtotal values → Tests TEST-001 → Validates REQ-001
Test Reference: TEST-001
Requirement: REQ-001
Assertion: Total == sum of all OrderLine.Subtotal
Approach: Implement Total as computed property on Order aggregate that sums all OrderLine.Subtotal values

Planning Step Reference: Step 2: Order must emit Order-Created with OrderId == order_id → Tests TEST-002 → Validates REQ-002
Test Reference: TEST-002
Requirement: REQ-002
Assertion: Order-Created emitted with OrderId == order_id
Approach: Emit Order-Created domain event from Order aggregate constructor with all required payload fields
```

---

# Next Step: Optional Visual Mapping

After this task completes and ALL tasks in the Plan are COMPLETE:

**Optional**: Apply visual mapping to completed requirements for validation and documentation.
- See [VISUAL-MAPPING-DISCIPLINE.md](../../VISUAL-MAPPING-DISCIPLINE.md#visual-mapping-in-post-task-phase) for post-task visual mapping guidance
- Create visual maps at: `visual-mapping/REQ-{{REQ_ID}}-MAPPING.md` (for each requirement)
- Visual maps validate requirement clarity and test coverage without modifying immutable tests
- This step is OPTIONAL; visual mapping is supplementary documentation

---

# TDD Verification Checklist (Pre-Completion)

**REQUIRED validations before marking Status = COMPLETE:**

### If Phase = [RED]:
- ✅ Test file created: `/tests/{{FEATURE_NAME}}/{{TEST_ID}}.md` exists
- ✅ Test written: File contains Assertions section with ONE failing test
- ✅ Test is failing: Run test and confirm it FAILS (proving test is real)
- ✅ Commit signed: `git log --oneline | head -1` shows `test(…): [RED]` message
- ✅ No test mutations: If previous [RED]-[IMPL] cycles exist, no tests were modified between cycles

### If Phase = [IMPL]:
- ✅ Paired [RED] test exists: Previous TASK with Phase=[RED] was completed
- ✅ Implementation written: Code file updated (NOT test files)
- ✅ Test passes: Run test for paired [RED] task — must pass ✓
- ✅ No test changes: Test file is identical to [RED] commit (diff shows ZERO changes)
- ✅ Commit signed: `git log --oneline | head -1` shows `impl(…):` message
- ✅ Implementation minimal: Code makes THIS test pass only; no extra features

**Violation**: If any checkbox fails → Status must remain NOT STARTED or IN PROGRESS. Do NOT set Status=COMPLETE until all pass.

---

# Completion State
**Status MUST be one of these EXACT values** (case-sensitive):
- `NOT STARTED`
- `IN PROGRESS`
- `COMPLETE`

```
Status: [EXACT_VALUE from list above]
Completed By: [ONLY fill if Status==COMPLETE; must be name of person/LLM who completed; leave BLANK if NOT STARTED or IN PROGRESS]
Completed On: [ONLY fill if Status==COMPLETE; ISO 8601 format YYYY-MM-DD; leave BLANK if NOT STARTED or IN PROGRESS]
```

**Structural Rules** (strictly enforced):
- If Status is `NOT STARTED` or `IN PROGRESS`, Completed By MUST be blank (empty/removed)
- If Status is `NOT STARTED` or `IN PROGRESS`, Completed On MUST be blank (empty/removed)
- If Status is `COMPLETE`, BOTH Completed By AND Completed On MUST be filled (no blanks)
- `Completed By` example: "Engineer-Jane-Smith" or "Claude-Haiku-4.5"
- `Completed On` example: "2026-03-01"
- Do NOT invent values for Completed By/On if Status != COMPLETE

**Completion Criteria** (MUST satisfy ALL before setting Status=COMPLETE):
1. All test cases in "Scenarios and Tests Covered" section have passing assertions
2. All invariants from referenced tests are preserved after task execution
3. All domain events from referenced tests are emitted with correct payloads
4. All requirements from "Requirements" section are satisfied
5. Zero violations of SpecForge Contract
6. Zero infrastructure references in approach descriptions

---

# Task Update Rules
When updating this task (and ONLY when updating this task):

**Fields that MAY be updated:**
- Status field (NOT STARTED → IN PROGRESS → COMPLETE progression only)
- Completed By field (filled ONLY when Status becomes COMPLETE)
- Completed On field (filled ONLY when Status becomes COMPLETE)

**Fields that MUST NEVER be updated:**
- {{TASK_ID}} (task number)
- {{FEATURE_NAME}} (feature name in header)
- Task Description section (do NOT revise after initial creation)
- Requirements section (do NOT add/remove requirements)
- Scenarios and Tests Covered section (do NOT add/remove tests)
- Traceability section (do NOT modify references)

**Synchronization Rule** (CRITICAL):
- Update this task's entry in {{PLAN_ID}}.md Task Index IMMEDIATELY when Status changes
- Exact sync format: `- TASK-{{TASK_ID}}: [title] \u2014 [EXACT_STATUS]` (must match Status field above)
- Do NOT create new tasks or modify other tasks
- Do NOT reorder tasks in Plan

**Example** (CORRECT sync):
- Plan Task Index: `- TASK-001: Order.Total calculation \u2014 IN PROGRESS`
- Task Status field: `Status: IN PROGRESS` (matches exactly)

**Violation examples** (WRONG):
- Changing task number (TASK-001 → TASK-002)
- Modifying task description after creation
- Adding new test cases to Scenarios and Tests Covered
- Part-filled Completed By/On (both must exist together or both blank)
- Status mismatch between Plan Index and Task Status field

---

# Traceability
This Task MUST reference ALL of the following:

**All Requirement IDs this task implements:**
- {{REQ_ID_1}} from /requirements/{{REQ_ID_1}}.md
- {{REQ_ID_2}} from /requirements/{{REQ_ID_2}}.md
- (verify all files exist before saving this task)

**All Scenario IDs this task supports:**
- {{SCENARIO_ID}} from /features/{{FEATURE_NAME}}/{{SCENARIO_ID}}.feature
- (verify file exists before saving this task)

**All Test IDs that validate this task:**
- {{TEST_ID_1}} from /tests/{{FEATURE_NAME}}/{{TEST_ID_1}}.md
- {{TEST_ID_2}} from /tests/{{FEATURE_NAME}}/{{TEST_ID_2}}.md
- (verify all files exist before saving this task)

**Strict Rules**:
- All IDs MUST be copied from {{PLAN_ID}}.md \u2014 do NOT invent or modify
- All referenced files MUST exist (fatal error if any file missing)
- This is a bidirectional constraint: Task \u2194 Plan \u2194 Tests \u2194 Scenarios \u2194 Requirements \u2194 Context
- If any link is broken, task is invalid

---

# Next Task Directive
After updating Status for this task, perform the following sequence:

**Step 1: Update Plan Index** (in {{PLAN_ID}}.md)
- Locate Task Index section
- Find this task's entry: `- TASK-{{TASK_ID}}: [title] \u2014 [OLD_STATUS]`
- Update to: `- TASK-{{TASK_ID}}: [title] \u2014 [NEW_STATUS]` (must match Status field above)
- Do NOT modify task number, title, or any other task

**Step 2: Identify Next Task** (if current task is now COMPLETE)
- Read the Plan's Task Index section
- Scan all tasks and find: `MIN(TASK-NNN where Status == "NOT STARTED")`
- This means: Find the lowest-numbered task with status "NOT STARTED"
- Example: If TASK-001=COMPLETE, TASK-002=NOT STARTED, TASK-003=COMPLETE, then next is TASK-002
- If ALL tasks are COMPLETE, stop \u2014 no more work

**Step 3: Create Next Task File** (if NOT STARTED task exists)
- Create new file: `/tasks/{{FEATURE_NAME}}/TASK-NNN.md` (where NNN = result from Step 2)
- Use `/tasks/{{FEATURE_NAME}}/TASK-{{TASK_ID}}.template.md` template
- Populate using values from Plan: copy {{SCENARIO_ID}}, {{TEST_ID_X}}, {{REQ_ID_X}}, {{PLAN_ID}}
- Set Status: NOT STARTED in new task
- Leave Completed By and Completed On blank

**Step 4: Update Plan Task Index** (after creating new task)
- Update new task's entry: `- TASK-NNN: [title] \u2014 NOT STARTED` (mark as created)
- Do NOT update any other tasks

**Automatic Progression** (REQUIRED):
- Do NOT ask user which task to work on
- Automatically create the next NOT STARTED task without user confirmation
- This is the final artefact in the SpecForge chain
