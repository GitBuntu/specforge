# 🚫 STOP — THIS IS A SPECFORGE TEMPLATE

This file is a **TEMPLATE**, not an artefact.

**LLM and engineer rules:**
- Do NOT edit this file.
- Do NOT fill in this file.
- Do NOT update this file.
- Do NOT continue this file.
- Do NOT “start work” in this file.
- Do NOT generate content inside this file.
- Do NOT overwrite this file.

When asked to use this template:
1. READ ONLY.
2. Create a **NEW** file in the correct folder.
3. Populate the **NEW** file.
4. Leave this template unchanged forever.

## Strict Sequencing Rule
- **DO NOT create implementation files until the complete SpecForge chain is defined.**
- The chain MUST be completed in this exact order:
  1. Context (01-*.md)
  2. Requirement (REQ-*.md) 
  3. Feature (FEATURE-*.md)
  4. Scenario (SCENARIO-*.md)
  5. Test (TEST-*.md)
  6. Plan (PLAN-*.md)
  7. Tasks (TASK-*.md)
- Implementation code is created ONLY AFTER all 7 SpecForge artifacts exist.
- Any deviation from this sequence violates the template.

---



## Feature: {{FEATURE_NAME}}

**Location**: `features/{{FEATURE_NAME}}/spec.md`

---

## 🔗 SpecForge Chain Position
**This is artefact 3 of 7.**

## 🔗 SpecForge Chain
Context → Requirements (1..N) → **Feature** → Scenario → Test → Plan → Tasks  
                                   ↑ You are here

---

## Implements Requirements
List all requirements this feature implements (copy IDs from `../../requirements/`):

- {{REQ_ID_1}} (from `../../requirements/REQ-{{REQ_ID_1}}.md`)
- {{REQ_ID_2}} (from `../../requirements/REQ-{{REQ_ID_2}}.md`)
- {{REQ_ID_3}} (from `../../requirements/REQ-{{REQ_ID_3}}.md`)

---

## Behavior
This feature implements the following atomic domain behaviours.  
**Copy requirement text VERBATIM from the referenced requirements below — do not paraphrase, interpret, or add detail.**

- {{REQ_ID_1}}: [COPY verbatim from REQ-{{REQ_ID_1}}.md — do not change a single word]
- {{REQ_ID_2}}: [COPY verbatim from REQ-{{REQ_ID_2}}.md — do not change a single word]
- {{REQ_ID_3}}: [COPY verbatim from REQ-{{REQ_ID_3}}.md — do not change a single word]

## Invariants
These MUST be copied verbatim from the Context.  
List ONLY the invariant names that the requirements above **enforce or preserve**.  
**Do not list invariants unrelated to the implemented requirements. Do not paraphrase or add detail.**

When this feature executes, these invariants MUST remain true:
- [INVARIANT_NAME] — from Context (enforced by {{REQ_ID_X}})
- [INVARIANT_NAME] — from Context (enforced by {{REQ_ID_X}})

## Domain Events
These MUST be copied verbatim from the Context.  
List ONLY the event names that the requirements above **emit or trigger**.  
**Do not list events unrelated to the implemented requirements. Do not paraphrase or add detail.**

When this feature executes its requirements, these events are emitted:
- [EVENT_NAME] — from Context (emitted by {{REQ_ID_X}})
- [EVENT_NAME] — from Context (emitted by {{REQ_ID_X}})

---

## SpecForge Rules
- Must reference **one or more** requirements in Implements Requirements.
- All referenced requirements must exist in `/requirements`.
- Must not introduce new invariants or events beyond those from the Context.
- All invariants listed here must be supported by at least one requirement.
- All events listed here must be triggered by at least one requirement.
- Scenarios in this feature must use @tags matching the requirement IDs listed here.

---

## Next Step Directive
Create scenario files in the **same directory** as this spec.md file.

**Scenario file path**: `{{FEATURE_NAME}}/SCENARIO-{{SCENARIO_NUMBER}}.feature` (sequential: 001, 002, 003...)

**Scenario tags**: Each scenario MUST use @tags matching requirement IDs from Implements Requirements above.

**Coverage**: Each scenario MUST reference ≥1 requirement ID from Implements Requirements section.

**Validation before proceeding**:
- Verify all {{REQ_ID_X}} files exist in `../../requirements/`
- Verify Context file exists at `../../domain/00-context.md`
- Verify all Invariants and Events listed above exist in Context
- Verify zero new invariants or events (copy-only from Context)
