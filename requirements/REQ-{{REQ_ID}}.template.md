# 🚫 STOP — THIS IS A SPECFORGE TEMPLATE

This file is a **TEMPLATE**, not an artefact.

**LLM and engineer rules:**
- Do NOT edit this file.
- Do NOT fill in this file.
- Do NOT update this file.
- Do NOT continue this file.
- Do NOT “start work” in this file.
- Do NOT generate content inside this file.
- Do NOT overwrite this file.

When asked to use this template:
1. READ ONLY.
2. Create a **NEW** file in the correct folder.
3. Populate the **NEW** file.
4. Leave this template unchanged forever.

## Strict Sequencing Rule
- **DO NOT create implementation files until the complete SpecForge chain is defined.**
- The chain MUST be completed in this exact order:
  1. Context (01-*.md)
  2. Requirement (REQ-*.md) 
  3. Feature (FEATURE-*.md)
  4. Scenario (SCENARIO-*.md)
  5. Test (TEST-*.md)
  6. Plan (PLAN-*.md)
  7. Tasks (TASK-*.md)
- Implementation code is created ONLY AFTER all 7 SpecForge artifacts exist.
- Any deviation from this sequence violates the template.
---



# Requirement {{REQ_ID}}

---

## 🔗 SpecForge Chain Position
**This is artefact 2 of 7.**

## 🔗 SpecForge Chain
Context → **Requirements (1..N)** → Feature → Scenario → Test → Plan → Tasks  
              ↑ You are here

---

## EARS
Choose ONE pattern below that best fits this requirement.  
**Replace placeholders in SQUARE BRACKETS with domain-specific values from the Context. Do not paraphrase or add extra detail.**

**Use the aggregate or entity name from Context, not a system name or generic term.**

**Ubiquitous:** The [aggregate/entity name from Context] shall [specific action/constraint: e.g., "maintain a total amount", "enforce positive quantity"]

**State driven:** While [specific state from Context: e.g., "Order.Status = Pending"], the [aggregate name] shall [specific action/constraint]

**Event driven:** When [specific event name from Context] occurs, the [aggregate name] shall [specific response/action]

<!-- **Optional feature:** Where [feature flag/condition], the [aggregate name] shall [specific behavior] -->

**Unwanted behaviour:** If [specific invalid input/state from business rules], then the [aggregate name] shall [specific error response/rejection]

**Complex:** While [state condition], when [trigger event], the [aggregate name] shall [specific behavior/constraint]

---

## Requirement Statement
Write the complete EARS requirement using ONE of the patterns above.  
**This is the atomic rule that the system must enforce. It must be testable and domain-grounded.**

[INSERT COMPLETE EARS REQUIREMENT HERE — e.g., "When a customer places an order, the Order aggregate shall calculate Total as the sum of all OrderLine amounts" or "While Order.Status = Pending, the Order aggregate shall reject modifications to items"]

---

## Invariants Preserved
List ONLY the invariant names from the Context that this requirement **preserves** (keeps true after execution).  
**This requirement MUST NOT introduce new invariants or add new constraints. It MUST only ensure existing invariants remain satisfied.**

List the invariants that remain true after this requirement executes:
- [INVARIANT_NAME_1] — from Context
- [INVARIANT_NAME_2] — from Context

---

## Domain Events Triggered
List ONLY the event names from the Context that this requirement **causes to be emitted**.  
**Do NOT invent new events. Copy exact names from Context. An empty list is valid if this requirement does not trigger events.**

When this requirement is satisfied, these domain events are emitted:
- [EVENT_NAME_1] — from Context
- [EVENT_NAME_2] — from Context

---

## SpecForge Rules
- Must reference ≥1 invariant from the context OR ≥1 domain event from the context (or both).
- Must not introduce new invariants or events.
- All referenced invariants and events must exist in the Context — do not invent them.

---

## Next Step Directive
Once all atomic requirements for this feature are written:

1. Verify each requirement:
   - Is it a single, unambiguous rule (not multiple rules combined)?
   - Does it reference ≥1 invariant OR ≥1 event from the Context?
   - Is it testable and measurable?

2. Create a feature folder named `{{FEATURE_NAME}}` under `/features/`.

3. Inside that folder, create `spec.md` using the feature template.

4. The feature **must reference all requirement IDs for this feature**.
